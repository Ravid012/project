# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This app targets Expo SDK 54 so it runs in App Store Expo Go (SDK 54). Do not upgrade the Expo SDK unless Expo Go on the store supports the newer SDK.
